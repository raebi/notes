# This are my new learned hacks with VIM

## sorting and make unique lines in VIM with external sort -u

```vim
:%! sort -u # all lines in document

:3,6! sort -u # sorting only lines 3 include 6

```
